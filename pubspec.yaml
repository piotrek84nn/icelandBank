name: iceland
description: Bla bla bla.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^0.13.4
  json_annotation: ^4.0.1
  flutter_bloc: ^7.3.0
  equatable: ^2.0.3
  intl: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  json_serializable: ^4.1.4
  build_runner: ^2.1.2

flutter:
  uses-material-design: true

  assets:
    - assets/images/work_in_progress.gif
    - assets/images/background.jpeg

